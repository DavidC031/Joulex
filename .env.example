# Local development defaults.
# Azure App Service injects PORT automatically, so do not hardcode PORT there unless needed.
PORT=3000
NODE_ENV=development

# Demo login credentials used by the local mock auth endpoint.
DEMO_EMAIL=demo@joulex.co
DEMO_PASSWORD=demo123

